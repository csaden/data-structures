queue.enqueue('a');
	queue.dequeue();
	queue.size();
	queue.enqueue('b');
	queue.dequeue();
	queue.size();
	queue.enqueue('c');
	queue.dequeue();
	queue.size();
	queue.enqueue('c');
	queue.enqueue('c');
	queue.enqueue('c');
	queue.enqueue('c');
	queue.enqueue('c');
	queue.enqueue('c');
	queue.enqueue('c');
	queue.enqueue('c');
	queue.dequeue();
	queue.dequeue();
	queue.dequeue();
	queue.dequeue();
	queue.dequeue();
	queue.dequeue();
	queue.dequeue();
	queue.size();
	queue.size();
	queue.size();
	queue.size();
	queue.size();
	queue.size();
	queue.enqueue('c');
	queue.enqueue('c');
	queue.enqueue('c');
	queue.dequeue();
	queue.dequeue();
	queue.dequeue();
	queue.enqueue('c');
	queue.enqueue('c');
	queue.enqueue('c');
	queue.dequeue();
	queue.dequeue();
	queue.dequeue();
	queue.dequeue();
	queue.dequeue();
	queue.enqueue('c');
	queue.enqueue('c');
	queue.enqueue('c');
	queue.enqueue('c');
	queue.enqueue('c');
	queue.enqueue('c');
	queue.enqueue('c');
	queue.enqueue('c');
	queue.dequeue();
	queue.dequeue();
	queue.dequeue();
	queue.dequeue();
	queue.size();
	queue.size();
	queue.size();
	queue.size();
	queue.size();